-- DEPRECATED — do not run.
-- Department and job data are read live from HRMIS (tbldepartment, tblemployee).
-- LMS only stores course_departments / course_professions mapping rows.
-- If lib_departments or lib_professions exist in db_lms, run migration_phase2_drop_lib_lookups.sql instead.

SELECT 1;
